plugins {
  id "com.github.node-gradle.node" version "2.2.4"
}

task startFrontend(type: NpmTask) {
  args = ['run', 'start']
}

task buildFrontend(type: NpmTask) {
  args = ['run', 'build-prod']
}

startFrontend.dependsOn(npmInstall)
buildFrontend.dependsOn(npmInstall)

